import React from 'react';

import { ReturnComponentType } from '../../types';

const Profile: React.FC = (): ReturnComponentType => (
  <div>
    <h1>Profile</h1>
  </div>
);

export default Profile;
