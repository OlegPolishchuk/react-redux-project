import React from 'react';

import { ReturnComponentType } from '../../types';

const PassRecovery: React.FC = (): ReturnComponentType => (
  <div>
    <h1>Password Recovery</h1>
  </div>
);

export default PassRecovery;
