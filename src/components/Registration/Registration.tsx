import React from 'react';

import { ReturnComponentType } from '../../types';

const Registration: React.FC = (): ReturnComponentType => (
  <div>
    <h1>Sign up</h1>
  </div>
);

export default Registration;
