import React from 'react';

import { ReturnComponentType } from '../../types';

const NewPass: React.FC = (): ReturnComponentType => (
  <div>
    <h1>New password</h1>
  </div>
);

export default NewPass;
